/* import React, {useState} from "react";
//  import Todolist from "../Todolist/index";
//import Data from './index';

export default function Status(props) {
  
      //console.log(props.status.filter((cheak)=>cheak.Status == "ToDo")) 

      //props.status.filter((cheak)=>cheak.status == "ToDo")

      const todocheak = props.status.filter((cheak)=>cheak.Status == "ToDo");
      const inprogresscheak = props.status.filter((cheak)=>cheak.Status == "Inprogress");
      const donecheak = props.status.filter((cheak)=>cheak.Status == "Done");
        


    
  



  return (
    <>
     
    </>
  );
}
 */